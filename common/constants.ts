export const BUCKET_ROOT = "https://storage.googleapis.com/static-covid";

export const STATIC_ROOT = "https://storage.googleapis.com/static-covid/static";

let all: { [key: string]: string } = { BUCKET_ROOT, STATIC_ROOT };
export default all;
